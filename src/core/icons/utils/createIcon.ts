import type { IconDefinition } from '@minimalist-ui/core';

/**
 * Creates an icon based on the given definition.
 * @param {IconDefinition} def - The definition of the icon.
 * @returns {IconDefinition} The created icon definition.
 */
export function createIcon(def: IconDefinition) {
    return def;
}
