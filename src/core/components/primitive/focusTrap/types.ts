export interface FocusTrapProps {
    /**
     * Indicates whether the focus trap should be active or not.
     */
    active?: boolean;

    /**
     * The children to render inside the focus trap.
     */
    children: React.ReactNode;
}